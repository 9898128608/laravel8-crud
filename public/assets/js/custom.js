$(function () {
    $(document).on("click", "#delete_patient_document", function () {
        // var id = $(this).data('id');
        // var post_url = "{{ route(deletePatientDocument) }}";
        // var datastring = 'id=' + id;
        // $.ajax({
        //     type: "POST",
        //     url: post_url,
        //     dataType: 'text',
        //     data: datastring
        // }).done(function (result) {

        // });
    });
});
